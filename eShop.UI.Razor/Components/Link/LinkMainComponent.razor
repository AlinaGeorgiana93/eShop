@using eShop.UI.Models.Link

<article>
    <div class="bg-success">@LinkGroup?.Name</div>
    <div>
        @foreach(var link in LinkGroup!.LinkOptions)
        {
            <LinkComponent LinkOption="link"/>
        }
    </div>
</article>

@code {
    [Parameter] public LinkGroup? LinkGroup { get; set; }
}
